import dotenv from 'dotenv'
import { huffmanEncoder } from './encoder'
import path from 'path'
import fs from 'fs'
import fsPromise from 'fs/promises'


dotenv.config()

export const getFile =async(req, res)=>{



}

export const uploadFile = async(req, res)=>{






}